# Global Variables

## global.plot
In UT/DR global.plot is used to determine where in the plot the game is in, 0 being the start. This value is a float.

### Example Use

    if (global.plot < 12)
        global.plot = 12;

## global.flag